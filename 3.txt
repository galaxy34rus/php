1. SELECT * FROM a, b WHERE a.id=b.a_id;
2. SELECT * FROM a JOIN b ON a.id=b.a_id;

Результат выполнения данных запросов один и тот же. Разница (помимо того, что WHERE является синтаксисом ANSI стандарта) лишь в том, что JOIN ON является более читаемым и гибким вариантом исполнения.